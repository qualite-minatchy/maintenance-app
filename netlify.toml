[build]
  command   = "npm install"
  functions = "netlify/functions"
  publish   = "."

[functions]
  included_files = ["node_modules/@netlify/blobs/**"]

[[redirects]]
  from   = "/api/*"
  to     = "/.netlify/functions/:splat"
  status = 200
